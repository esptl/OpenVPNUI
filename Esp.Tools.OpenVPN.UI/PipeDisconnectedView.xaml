<UserControl x:Class="Esp.Tools.OpenVPN.UI.PipeDisconnectedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Esp.Tools.OpenVPN.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="5">
        <Grid Style="{DynamicResource GridStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Image VerticalAlignment="Center" Width="22" Height="22" Source="/Icons/connect_no.png" />

            <StackPanel Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock FontSize="10" Foreground="White" TextWrapping="Wrap">
                    Waiting to connect to the <TextBlock Foreground="#FFD0FF9F" Text="OpenVPN Host Service" />. Check that the service is running on this machine.
                </TextBlock>
            </StackPanel>
           
        </Grid>

    </StackPanel>
</UserControl>
